<h1>Planets</h1>

<div>
  <% @ordered_planets.each do |planet| %>
    <div id=<%= "planet-#{planet.id}" %>
      <h3>Position: <%= planet.position %></h3>
      <p>Name: <%= link_to planet.name, planet_path(planet) %></p>
      <p>Info: <%= planet.info %></p>
      <p><%= "Has Rings!" if planet.has_rings %></p>
      <br/>
    </div>
  <% end %>
</div>
